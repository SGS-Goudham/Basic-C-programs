#include <stdio.h>

int main()
{
    int l[100000],n,sum=0;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&l[i]);
        sum+=l[i];
    }
    printf("%d",sum);
    return 0;
}
